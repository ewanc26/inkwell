<?xml version="1.0" encoding="utf-8"?>
<!--
  Inkwell Android app manifest.

  Declares INTERNET for AT Protocol and Constellation API access,
  POST_NOTIFICATIONS for background content-update polling (WorkManager),
  and the OAuth redirect URI (uk.ewancroft.inkwell:/callback).

  The autoVerify on the OAuth intent-filter enables Android Automatic
  Link Handling so the OS routes the OAuth callback back to Inkwell
  without a browser redirect page.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".InkwellApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inkwell">

        <!--
          InkwellApp.onCreate() initializes WorkManager itself (via
          HiltWorkerFactory, for Hilt-injected workers) — disable
          WorkManager's own automatic androidx.startup initializer, which
          otherwise runs first and makes the manual initialize() call
          throw "WorkManager is already initialized".
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              OAuth redirect: captures the PDS authorization callback.
              The path is part of the contract: do not route arbitrary
              uk.ewancroft.inkwell: links into the OAuth handler.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="uk.ewancroft.inkwell"
                    android:path="/callback" />
            </intent-filter>
        </activity>

        <!-- Grants temporary read access to the exported reading-data JSON
             file (Settings -> Export Data) to whatever app the user shares
             it to, without exposing the app's full storage. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
