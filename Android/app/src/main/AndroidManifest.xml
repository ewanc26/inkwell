<?xml version="1.0" encoding="utf-8"?>
<!--
  Inkwell Android app manifest.

  Declares INTERNET for AT Protocol and Constellation API access,
  POST_NOTIFICATIONS for background content-update polling (WorkManager),
  and the OAuth redirect URI (uk.ewancroft.inkwell:/callback).

  The autoVerify on the OAuth intent-filter enables Android Automatic
  Link Handling so the OS routes the OAuth callback back to Inkwell
  without a browser redirect page.

  android:localeConfig points at @xml/locales_config, which enables
  Android's system-managed per-app language picker (API 33+) instead of a
  parallel in-app locale store.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- These legacy permissions are implied by old library manifests in some
         dependency graphs, but Inkwell has no storage or phone-state feature
         that needs them. Keep them out of the merged release manifest. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove" />

    <application
        android:name=".InkwellApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/backup_rules_legacy"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Inkwell">

        <!--
          InkwellApp.onCreate() initializes WorkManager itself (via
          HiltWorkerFactory, for Hilt-injected workers) — disable
          WorkManager's own automatic androidx.startup initializer, which
          otherwise runs first and makes the manual initialize() call
          throw "WorkManager is already initialized".
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Explicit content hand-off links. OAuth remains isolated to
                 uk.ewancroft.inkwell:/callback above. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="inkwell" android:host="document" />
            </intent-filter>

            <!--
              Verified HTTPS hand-off (Android App Links): a canonical
              https://inkwell.ewancroft.uk/open?uri=<AT-URI> link, distinct
              from both the inkwell://document custom scheme above and the
              OAuth callback below. autoVerify requires the domain to serve
              /.well-known/assetlinks.json (see
              website/src/routes/.well-known/assetlinks.json/+server.ts).
              Unlike the custom scheme, a link arriving here is only an *unverified
              claim* about an AT-URI — HttpsDeepLinkPolicy/HttpsDeepLinkResolver
              (see the deeplink package) confirm it against the author's own
              PDS record and published discovery link before routing into the
              Reader, falling back to the browser otherwise.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="inkwell.ewancroft.uk"
                    android:pathPrefix="/open" />
            </intent-filter>

            <!--
              OAuth redirect: captures the PDS authorization callback.
              The path is part of the contract: do not route arbitrary
              uk.ewancroft.inkwell: links into the OAuth handler.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="uk.ewancroft.inkwell"
                    android:path="/callback"
                    tools:ignore="AppLinkUrlError" />
            </intent-filter>
        </activity>

        <!-- Grants temporary read access to the exported reading-data JSON
             file (Settings -> Export Data) to whatever app the user shares
             it to, without exposing the app's full storage. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
