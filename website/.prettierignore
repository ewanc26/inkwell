# Vendored Claude Code skill files — not this project's source, and
# reformatting them would just create needless drift from upstream.
.agents/

# Never reformat a lockfile.
pnpm-lock.yaml

# Fully generated by fdroidserver's `fdroid update` on every release;
# reformatting would be undone (and possibly broken) by the next run.
static/fdroid/repo/

# Exact mirror of iOS/altstore/source.json; formatting it would break the
# byte-for-byte release-manifest sync check below the formatter gate in CI.
static/altstore/source.json

# Generated by tools/legal/render.mjs from ../legal/*.md, and compared
# byte-for-byte by its --check mode in CI's legal-sync job. Reformatting it
# would make that check fail on a freshly regenerated file.
src/lib/legal/documents.ts
