# ── Inkwell website environment variables ────────────────────────
# Not currently consumed at build time (metadata lives in config.ts),
# but kept as a placeholder for any future server-side config.

PUBLIC_SITE_TITLE="Inkwell"
PUBLIC_SITE_DESCRIPTION="A native reader and writer for the Standard.site publishing ecosystem on AT Protocol."
