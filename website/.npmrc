# ── PNPM config ──────────────────────────────────────────────────
# Block installs on unexpected Node versions (project targets
# the same engine as the Vercel deployment runtime).

engine-strict=true
